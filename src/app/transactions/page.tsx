import PlaceholderPage from "../components/PlaceholderPage";

export default function TransactionsPage() {
  return (
    <PlaceholderPage
      title="Transactions"
      blurb="Every expense and every income entry, in one ledger."
    />
  );
}
